* Possibly send a md5 check sum of each chunk for the server to verify
* Compute the md5 check sum as the file is uploaded in parts, maybe store the partially computed checksum as a file?
* Include file size when recovering files
